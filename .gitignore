# OS / editor
.DS_Store
Thumbs.db
ocr_error.txt
ocr_output.txt
*.ps1

.firebase/

_debug_*

__pycache__/
*.pyc

_check_ci.py
_colorize_gamet.py
_ci_logs*

HAR Num Wx-model Output/

# Dependencies (installed locally, not needed in the repo)
node_modules/

# Local Firebase config with the real API key — the deploy workflow
# generates its own from a GitHub Actions secret (see deploy-pages.yml).
# Use config.example.js as the template.
config.js

# Build de l'application Android (Capacitor) — tout est régénéré par
# tools/build_www.py et le workflow build-android.yml, rien à versionner.
www/
android/
node_modules/
*.apk
