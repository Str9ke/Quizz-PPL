// Copy this file to `config.js` (DO NOT commit `config.js`)
// Fill in your real API key / secrets here.
// Example minimal content used by the app at runtime:
window.FIREBASE_CONFIG = {
  apiKey: "REPLACE_WITH_YOUR_API_KEY"
};
